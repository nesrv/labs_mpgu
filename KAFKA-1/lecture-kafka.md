# Apache Kafka 4.1 (KRaft Mode) - Лекция

---

## Слайд 1: Введение в Apache Kafka

**Apache Kafka** - распределенная платформа потоковой передачи данных

**Основные возможности:**
- Публикация и подписка на потоки событий
- Хранение потоков событий
- Обработка потоков событий в реальном времени

**Применение:** логирование, мониторинг, обмен сообщениями, event sourcing, stream processing

---

## Слайд 2: Зачем нужна Kafka?

**Проблемы традиционных систем:**
- Точка-точка интеграции (N×M связей)
- Сложность масштабирования
- Потеря сообщений при сбоях
- Низкая пропускная способность

**Решение Kafka:**
- Централизованная шина событий
- Горизонтальное масштабирование
- Гарантии доставки
- Высокая пропускная способность (миллионы сообщений/сек)

---

## Слайд 3: Основные концепции

**Producer** - отправляет сообщения в Kafka

**Consumer** - читает сообщения из Kafka

**Broker** - сервер Kafka, хранит данные

**Topic** - категория/канал для сообщений

**Partition** - часть топика для параллелизма

---

## Слайд 4: Архитектура Kafka

```
Producer → Broker (Topic/Partitions) → Consumer
           ↓
        Хранилище (диск)
```

**Ключевые особенности:**
- Распределенная система
- Репликация данных
- Отказоустойчивость
- Горизонтальное масштабирование

---

## Слайд 5: Topic и Partitions

**Topic** - логическая категория сообщений
- Аналог таблицы в БД
- Может содержать любые данные

**Partition** - физическое разделение топика
- Упорядоченная последовательность сообщений
- Каждое сообщение имеет offset (позицию)
- Партиции распределены по брокерам

```
Topic: orders
├── Partition 0: [msg1, msg2, msg3]
├── Partition 1: [msg4, msg5, msg6]
└── Partition 2: [msg7, msg8, msg9]
```

---

## Слайд 6: Offset

**Offset** - уникальный идентификатор сообщения в партиции

- Монотонно возрастающее число
- Уникален только в рамках партиции
- Consumer отслеживает свой offset
- Позволяет читать сообщения повторно

```
Partition 0: [0][1][2][3][4][5][6]
                     ↑
              Consumer offset = 3
```

---

## Слайд 7: Producer - отправка сообщений

**Основные параметры:**
- `bootstrap_servers` - адреса брокеров
- `key` - ключ для партиционирования
- `value` - само сообщение
- `serializer` - сериализация данных

**Гарантии доставки:**
- `acks=0` - не ждать подтверждения
- `acks=1` - подтверждение от лидера
- `acks=all` - подтверждение от всех реплик

---

## Слайд 8: Producer - пример кода

```python
from kafka import KafkaProducer
import json

producer = KafkaProducer(
    bootstrap_servers='localhost:19092',
    value_serializer=lambda v: json.dumps(v).encode('utf-8')
)

message = {'user_id': 123, 'action': 'login'}
producer.send('user-events', value=message)
producer.flush()
```

---

## Слайд 9: Партиционирование

**Как выбирается партиция?**

1. **С ключом:** `hash(key) % num_partitions`
   - Одинаковые ключи → одна партиция
   - Гарантия порядка для ключа

2. **Без ключа:** Round-robin или sticky partitioning
   - Равномерное распределение

```python
# Все заказы пользователя в одну партицию
producer.send('orders', key=user_id, value=order)
```

---

## Слайд 10: Consumer - чтение сообщений

**Основные параметры:**
- `bootstrap_servers` - адреса брокеров
- `group_id` - идентификатор группы
- `auto_offset_reset` - с какого места читать
- `enable_auto_commit` - автосохранение offset

**Режимы чтения:**
- `earliest` - с начала топика
- `latest` - только новые сообщения

---

## Слайд 11: Consumer - пример кода

```python
from kafka import KafkaConsumer
import json

consumer = KafkaConsumer(
    'user-events',
    bootstrap_servers='localhost:19092',
    group_id='analytics-service',
    value_deserializer=lambda m: json.loads(m.decode('utf-8')),
    auto_offset_reset='earliest'
)

for message in consumer:
    print(f"Offset: {message.offset}")
    print(f"Value: {message.value}")
```

---

## Слайд 12: Consumer Groups

**Consumer Group** - группа consumers, читающих один топик

**Принципы работы:**
- Каждая партиция читается только одним consumer в группе
- Автоматическая балансировка при добавлении/удалении consumers
- Разные группы читают независимо

```
Topic (3 partitions)
├── P0 → Consumer 1 (group A)
├── P1 → Consumer 2 (group A)
└── P2 → Consumer 3 (group A)

├── P0,P1,P2 → Consumer 1 (group B)
```

---

## Слайд 13: Rebalancing

**Rebalancing** - перераспределение партиций между consumers

**Когда происходит:**
- Добавление нового consumer
- Удаление/сбой consumer
- Изменение количества партиций

**Важно:** Во время rebalancing обработка останавливается

---

## Слайд 14: Commit Offset

**Commit** - сохранение текущей позиции чтения

**Стратегии:**
1. **Auto-commit** - автоматически каждые N секунд
2. **Manual commit** - явное сохранение после обработки

```python
# Manual commit
for message in consumer:
    process(message.value)
    consumer.commit()  # Сохранить offset
```

**At-least-once vs At-most-once vs Exactly-once**

---

## Слайд 15: Гарантии доставки

**At-most-once** (не более одного раза)
- Commit перед обработкой
- Возможна потеря сообщений

**At-least-once** (минимум один раз)
- Commit после обработки
- Возможны дубликаты

**Exactly-once** (ровно один раз)
- Transactional producer + idempotent consumer
- Самая сложная, но надежная

---

## Слайд 16: Репликация

**Replication Factor** - количество копий данных

```
Topic: orders (replication-factor=3)
Partition 0:
├── Leader (Broker 1) - чтение/запись
├── Follower (Broker 2) - реплика
└── Follower (Broker 3) - реплика
```

**ISR (In-Sync Replicas)** - реплики, синхронизированные с лидером

**Отказоустойчивость:** Если лидер падает, выбирается новый из ISR

---

## Слайд 17: Kafka 4.1 - KRaft Mode

**KRaft** = Kafka Raft (без Zookeeper!)

**Преимущества:**
- Упрощенная архитектура
- Быстрее запуск и восстановление
- Меньше зависимостей
- Лучшая масштабируемость

**Роли узлов:**
- **Controller** - управление метаданными
- **Broker** - хранение данных
- **Combined** - обе роли (для разработки)

---

## Слайд 18: KRaft vs Zookeeper

| Характеристика | Zookeeper | KRaft |
|----------------|-----------|-------|
| Компоненты | Kafka + Zookeeper | Только Kafka |
| Метаданные | В Zookeeper | В Kafka топике |
| Масштабируемость | До 200k партиций | Миллионы партиций |
| Время восстановления | Минуты | Секунды |
| Сложность | Высокая | Низкая |

---

## Слайд 19: Конфигурация KRaft

```yaml
KAFKA_NODE_ID: 1
KAFKA_PROCESS_ROLES: broker,controller
KAFKA_CONTROLLER_QUORUM_VOTERS: 1@localhost:9093

KAFKA_LISTENERS: 
  PLAINTEXT://0.0.0.0:9092
  EXTERNAL://0.0.0.0:19092
  CONTROLLER://0.0.0.0:9093

KAFKA_ADVERTISED_LISTENERS:
  PLAINTEXT://kafka:9092
  EXTERNAL://localhost:19092
```

---

## Слайд 20: Listeners в Kafka

**Listener** - точка подключения к Kafka

**PLAINTEXT** - внутри Docker сети (kafka:9092)
- Для связи между контейнерами

**EXTERNAL** - с хоста (localhost:19092)
- Для подключения с вашего компьютера

**CONTROLLER** - для KRaft протокола (9093)
- Для координации между узлами

---

## Слайд 21: Создание топиков

```bash
# Через CLI
docker exec -it kafka /opt/kafka/bin/kafka-topics.sh \
  --create \
  --topic orders \
  --bootstrap-server localhost:9092 \
  --partitions 3 \
  --replication-factor 1

# Через код (auto-create)
producer.send('new-topic', value=message)
```

**Параметры:**
- `partitions` - количество партиций
- `replication-factor` - количество реплик

---

## Слайд 22: Управление топиками

```bash
# Список топиков
kafka-topics.sh --list --bootstrap-server localhost:9092

# Описание топика
kafka-topics.sh --describe --topic orders \
  --bootstrap-server localhost:9092

# Удаление топика
kafka-topics.sh --delete --topic orders \
  --bootstrap-server localhost:9092
```

---

## Слайд 23: Kafka UI

**Kafka UI** - веб-интерфейс для управления Kafka

**Возможности:**
- Просмотр топиков и сообщений
- Мониторинг consumer groups
- Управление конфигурацией
- Просмотр метрик

**Доступ:** http://localhost:8080

---

## Слайд 24: Сериализация данных

**Serializer** - преобразование объекта в байты

**Популярные форматы:**
- **JSON** - читаемый, гибкий
- **Avro** - компактный, со схемой
- **Protobuf** - быстрый, типизированный
- **String** - простой текст

```python
# JSON serializer
value_serializer=lambda v: json.dumps(v).encode('utf-8')

# JSON deserializer
value_deserializer=lambda m: json.loads(m.decode('utf-8'))
```

---

## Слайд 25: Kafka Headers

**Headers** - метаданные сообщения (key-value пары)

**Применение:**
- Трейсинг (correlation_id)
- Маршрутизация
- Версионирование схем
- Аутентификация

```python
producer.send(
    'orders',
    value=order,
    headers=[
        ('correlation_id', b'12345'),
        ('source', b'api-service')
    ]
)
```

---

## Слайд 26: Kafka Streams

**Kafka Streams** - библиотека для обработки потоков

**Возможности:**
- Фильтрация и трансформация
- Агрегация по окнам времени
- Соединение потоков (joins)
- Stateful обработка

**KTable** - материализованное представление (changelog)

**KStream** - поток событий

---

## Слайд 27: Windowing

**Window** - временное окно для агрегации

**Типы окон:**
- **Tumbling** - фиксированные неперекрывающиеся (каждые 5 мин)
- **Hopping** - фиксированные с перекрытием (каждые 5 мин, сдвиг 1 мин)
- **Sliding** - динамические по событиям
- **Session** - по активности пользователя

```python
# Подсчет событий за 5 минут
stream.window_by(minutes=5).count()
```

---

## Слайд 28: Kafka Connect

**Kafka Connect** - фреймворк для интеграции с внешними системами

**Source Connector** - импорт данных в Kafka
- Базы данных (CDC)
- Файлы
- API

**Sink Connector** - экспорт данных из Kafka
- Elasticsearch
- S3
- Базы данных

---

## Слайд 29: Schema Registry

**Schema Registry** - централизованное хранилище схем данных

**Преимущества:**
- Версионирование схем
- Проверка совместимости
- Эволюция схем без даунтайма
- Уменьшение размера сообщений

**Форматы:** Avro, JSON Schema, Protobuf

---

## Слайд 30: Производительность Kafka

**Оптимизация Producer:**
- `batch.size` - размер батча (16KB по умолчанию)
- `linger.ms` - задержка перед отправкой (0 по умолчанию)
- `compression.type` - сжатие (gzip, snappy, lz4, zstd)

```python
producer = KafkaProducer(
    batch_size=32768,
    linger_ms=10,
    compression_type='lz4'
)
```

---

## Слайд 31: Производительность Consumer

**Оптимизация Consumer:**
- `fetch.min.bytes` - минимальный размер данных для fetch
- `fetch.max.wait.ms` - максимальное время ожидания
- `max.poll.records` - количество записей за poll

**Параллелизм:**
- Количество consumers ≤ количество партиций
- Больше партиций = больше параллелизма

---

## Слайд 32: Мониторинг Kafka

**Ключевые метрики:**

**Broker:**
- Throughput (сообщений/сек)
- Disk usage
- Network I/O

**Consumer:**
- Lag (отставание от последнего сообщения)
- Commit rate
- Rebalance frequency

**Producer:**
- Send rate
- Error rate
- Batch size

---

## Слайд 33: Consumer Lag

**Lag** - разница между последним сообщением и текущим offset consumer

```
Latest offset: 1000
Consumer offset: 950
Lag: 50 сообщений
```

**Причины lag:**
- Медленная обработка
- Недостаточно consumers
- Проблемы с сетью

**Решение:** Масштабирование consumers, оптимизация обработки

---

## Слайд 34: Retention Policy

**Retention** - политика хранения данных

**По времени:**
```bash
retention.ms=604800000  # 7 дней
```

**По размеру:**
```bash
retention.bytes=1073741824  # 1GB
```

**Log Compaction** - хранение только последнего значения для ключа

---

## Слайд 35: Transactional Producer

**Транзакции** - атомарная запись в несколько топиков

```python
producer = KafkaProducer(
    transactional_id='my-transactional-id',
    enable_idempotence=True
)

producer.init_transactions()
producer.begin_transaction()
try:
    producer.send('topic1', value=msg1)
    producer.send('topic2', value=msg2)
    producer.commit_transaction()
except:
    producer.abort_transaction()
```

**Exactly-once семантика**

---

## Слайд 36: Idempotent Producer

**Idempotence** - повторная отправка не создает дубликатов

```python
producer = KafkaProducer(
    enable_idempotence=True
)
```

**Механизм:**
- Producer ID
- Sequence number для каждого сообщения
- Broker отклоняет дубликаты

---

## Слайд 37: Dead Letter Queue (DLQ)

**DLQ** - топик для проблемных сообщений

**Сценарий:**
1. Consumer не может обработать сообщение
2. После N попыток отправляет в DLQ
3. Ручная обработка или анализ

```python
try:
    process(message)
except Exception as e:
    if retry_count >= 3:
        producer.send('dlq-topic', value=message)
```

---

## Слайд 38: Микросервисная архитектура с Kafka

**Паттерны:**

**Event-Driven Architecture**
- Сервисы общаются через события
- Слабая связанность

**CQRS (Command Query Responsibility Segregation)**
- Разделение чтения и записи
- Kafka для синхронизации

**Event Sourcing**
- Хранение всех изменений как события
- Восстановление состояния из истории

---

## Слайд 39: Best Practices

**Producer:**
- Используйте ключи для партиционирования
- Включайте idempotence
- Настраивайте acks по требованиям

**Consumer:**
- Используйте consumer groups
- Обрабатывайте ошибки (DLQ)
- Мониторьте lag

**Топики:**
- Достаточное количество партиций
- Правильный replication factor (≥3 для prod)
- Настройте retention

---

## Слайд 40: Резюме

**Apache Kafka 4.1 (KRaft):**
- ✅ Распределенная платформа потоковой обработки
- ✅ Высокая пропускная способность и масштабируемость
- ✅ Отказоустойчивость через репликацию
- ✅ Гибкие гарантии доставки
- ✅ Упрощенная архитектура без Zookeeper
- ✅ Подходит для event-driven архитектуры

**Готовы к лабораторной работе!** 🚀

---

## Дополнительные ресурсы

- Официальная документация: https://kafka.apache.org/documentation/
- Kafka Improvement Proposals (KIP): https://cwiki.apache.org/confluence/display/KAFKA/Kafka+Improvement+Proposals
- Confluent Blog: https://www.confluent.io/blog/
- Книга "Kafka: The Definitive Guide"
