# 📊 Методичка по работе с Kafka UI

## Веб-интерфейс для управления Apache Kafka

---

## 🎯 Цель методички

Изучить возможности веб-интерфейса **Kafka UI** для визуального управления кластером Apache Kafka, мониторинга топиков, просмотра сообщений и работы с consumer groups.

---

## 📚 Что такое Kafka UI?

**Kafka UI** (от Provectus Labs) — это современный веб-интерфейс с открытым исходным кодом для управления и мониторинга Apache Kafka. Он предоставляет удобный графический интерфейс для работы с Kafka без необходимости использования командной строки.

### Основные возможности:

- ✅ Просмотр и управление топиками
- ✅ Чтение и отправка сообщений
- ✅ Мониторинг consumer groups
- ✅ Просмотр метрик и статистики
- ✅ Создание и удаление топиков
- ✅ Управление схемами (Schema Registry)
- ✅ Просмотр конфигурации брокеров

---

## 🔧 Подготовка

### Предварительные требования

1. Запущенный кластер Kafka (через Docker Compose)
2. Kafka UI должен быть доступен на `http://localhost:8080`

### Проверка доступности

Убедитесь, что контейнеры запущены:

```bash
docker-compose ps
```

Должны быть запущены:

- `kafka` (порт 9092)
- `kafka-ui` (порт 8080)

Если контейнеры не запущены:

```bash
cd KAFKA-1
docker-compose up -d
```

---

## 🌐 Первый запуск и подключение

### 1. Открытие веб-интерфейса

Откройте браузер и перейдите по адресу:

```
http://localhost:8080
```

### 2. Главная страница

При первом открытии вы увидите:

- **Список кластеров** — отображается кластер "local"
- **Статус подключения** — должен быть зелёный индикатор (▲) в верхней панели
- **Информация о кластере** — версия Kafka, количество брокеров
- **Временная метка** — показывает время последнего обновления данных

### 3. Структура интерфейса

Kafka UI имеет современный и интуитивный интерфейс с тёмной темой:

#### Верхняя панель (Header)

- **Логотип и название** — "UI for Apache Kafka" с иконкой
- **Индикатор статуса** — зелёный треугольник (▲) показывает здоровье кластера
- **Идентификатор кластера** — commit hash и временная метка последнего обновления

#### Левая панель навигации (Sidebar)

Меню навигации предоставляет быстрый доступ к основным разделам:

- 🏠 **Dashboard** — общая информация о кластере, метрики
- 📦 **local** (или имя кластера) — выпадающий список с информацией о кластере
  - Красная точка указывает на активный кластер
  - Можно переключаться между несколькими кластерами
- ⚙️ **Brokers** — список брокеров, их статус и конфигурация
- 📝 **Topics** — управление топиками (выделяется при активном разделе)
- 👥 **Consumers** — просмотр consumer groups и их lag
- 📊 **Schema Registry** — управление схемами (если настроен)

#### Основная область (Main Content Area)

- **Хлебные крошки (Breadcrumbs)** — показывают текущий путь, например: "Topics / Create"
- **Формы и таблицы** — интуитивно понятные элементы управления
- **Кнопки действий** — цветовое кодирование:
  - Фиолетовые кнопки — основные действия (Create, Save)
  - Серые/прозрачные кнопки — вторичные действия (Cancel)

### 4. Особенности интерфейса

#### Удобство использования

- ✅ **Визуальная обратная связь** — активный раздел подсвечивается
- ✅ **Понятные иконки** — быстрая ориентация в интерфейсе
- ✅ **Цветовое кодирование** — зелёный = хорошо, красный = проблема
- ✅ **Быстрые действия** — кнопки для частых операций (например, выбор времени хранения)
- ✅ **Валидация форм** — обязательные поля помечены звёздочкой (\*)

#### Адаптивность

- Интерфейс адаптируется под размер экрана
- Удобно работать как на десктопе, так и на планшете

#### Информативность

- Подсказки и описания для сложных параметров
- Отображение метрик в реальном времени
- Детальная информация о каждом объекте (топик, consumer group, брокер)

---

## 🔍 Детальный анализ интерфейса создания топика

### Структура формы создания топика

На основе анализа интерфейса Kafka UI, форма создания топика организована следующим образом:

#### 1. Заголовок и навигация

- **Хлебные крошки**: "Topics / Create" — показывает текущее местоположение
- Позволяет легко вернуться к списку топиков

#### 2. Группировка полей

Поля формы логически сгруппированы:

**Основные параметры:**

- Topic Name, Number of Partitions — обязательные базовые настройки

**Политики и репликация:**

- Cleanup policy, Replication Factor, Min In Sync Replicas — настройки надёжности

**Ограничения:**

- Time to retain data, Max size on disk, Maximum message size — ограничения ресурсов

**Расширенные настройки:**

- Custom parameters — для опытных пользователей

#### 3. Умные элементы управления

**Кнопки быстрого выбора времени хранения:**

- "12 hours", "1 day", "2 days", "7 days", "4 weeks"
- Избавляют от необходимости вычислять миллисекунды вручную
- Показывают типичные сценарии использования

**Выпадающие списки:**

- Cleanup policy — выбор из предопределённых вариантов
- Max size on disk — "Not Set" или конкретное значение

#### 4. Визуальная иерархия

- **Обязательные поля** помечены звёздочкой (\*)
- **Placeholder текст** подсказывает формат ввода
- **Кнопки действий** различаются по цвету и размеру:
  - "Cancel" — вторичное действие
  - "Create topic" — основное действие (более тёмный фиолетовый)

### Преимущества такого дизайна

1. **Прогрессивное раскрытие сложности**

   - Базовые поля видны сразу
   - Расширенные настройки доступны, но не мешают новичкам

2. **Предотвращение ошибок**

   - Валидация обязательных полей
   - Подсказки для правильного заполнения
   - Разумные значения по умолчанию

3. **Эффективность работы**

   - Быстрые кнопки для частых операций
   - Не нужно запоминать точные значения (например, миллисекунды)

4. **Гибкость**
   - Custom parameters позволяют настроить любые параметры Kafka
   - Подходит как для новичков, так и для экспертов

---

## 📝 Работа с топиками (Topics)

### Просмотр списка топиков

1. В меню слева выберите **"Topics"**
2. Отобразится список всех топиков в кластере
3. Для каждого топика показывается:
   - Имя топика
   - Количество партиций
   - Фактор репликации
   - Размер данных
   - Количество сообщений

### Просмотр детальной информации о топике

1. Кликните на имя топика
2. Откроется страница с детальной информацией:
   - **Overview** — общая информация
   - **Messages** — просмотр сообщений
   - **Consumers** — подключенные consumer groups
   - **Settings** — настройки топика
   - **Partitions** — информация о партициях

### Создание нового топика

#### Способ 1: Через интерфейс Kafka UI

1. На странице **Topics** нажмите кнопку **"+ Add a Topic"** или **"Create Topic"**
2. Откроется форма создания топика с детальными настройками

#### Детальное описание формы создания топика

**Обязательные поля (помечены звёздочкой \*):**

- **Topic Name \*** — имя топика (например: `my-test-topic`)

  - Должно быть уникальным в кластере
  - Может содержать буквы, цифры, точки, дефисы и подчёркивания

- **Number of Partitions \*** — количество партиций
  - Рекомендуется: **1** для обучения и standalone режима
  - Для production: обычно 3-6 партиций на топик
  - Больше партиций = больше параллелизма, но больше overhead

**Дополнительные настройки:**

- **Cleanup policy** — политика очистки старых сообщений

  - **Delete** (по умолчанию) — удаление сообщений по времени или размеру
  - **Compact** — сжатие: сохраняются только последние значения для каждого ключа
  - Выбор зависит от типа данных (логи vs. состояние)

- **Replication Factor** — фактор репликации

  - **1** — для standalone/обучения (одна копия данных)
  - **3** — для production (рекомендуется для отказоустойчивости)
  - Не может быть больше количества брокеров

- **Min In Sync Replicas** — минимальное количество синхронизированных реплик

  - Гарантирует, что producer получит подтверждение только после записи в N реплик
  - Для standalone: обычно **1**
  - Для production: обычно **2** (при replication factor 3)

- **Time to retain data (in ms)** — время хранения данных в миллисекундах

  - Быстрый выбор: кнопки **"12 hours"**, **"1 day"**, **"2 days"**, **"7 days"**, **"4 weeks"**
  - Или введите значение вручную
  - По умолчанию: **7 дней** (604800000 мс)
  - `-1` означает "бесконечно" (только для compact policy)

- **Max size on disk in GB** — максимальный размер топика на диске

  - По умолчанию: **"Not Set"** (неограниченно)
  - При достижении лимита старые сообщения удаляются

- **Maximum message size in bytes** — максимальный размер одного сообщения

  - По умолчанию: **1000000** (1 МБ)
  - Увеличьте для больших сообщений (например, изображения, файлы)

- **Custom parameters** — дополнительные параметры конфигурации
  - Кнопка **"+ Add Custom Parameter"** для расширенных настроек
  - Позволяет задать любые параметры Kafka в формате `key=value`
  - Примеры:
    - `compression.type=gzip` — сжатие сообщений
    - `min.insync.replicas=2` — минимальные синхронизированные реплики
    - `segment.ms=3600000` — время создания нового сегмента

3. После заполнения формы нажмите **"Create Topic"** (фиолетовая кнопка)
4. Или **"Cancel"** для отмены создания

#### Рекомендуемые настройки для обучения

Для учебных целей используйте следующие значения:

```
Topic Name: my-test-topic
Number of Partitions: 1
Cleanup policy: Delete
Replication Factor: 1
Min In Sync Replicas: 1
Time to retain data: 7 days (или 1 day для экономии места)
Max size on disk: Not Set
Maximum message size: 1000000 (1 МБ)
Custom parameters: (не требуется)
```

#### Способ 2: Через командную строку

```bash
docker exec -it kafka /opt/kafka/bin/kafka-topics.sh   --create   --topic my-test-topic-2   --bootstrap-server localhost:9092   --partitions 1   --replication-factor 1
```

После создания топик topic my-test-topic-2появится в списке топиков в Kafka UI.

### Удаление топика

> ⚠️ **Внимание:** Удаление топика необратимо! Все данные будут потеряны.

1. Найдите топик в списке
2. Выделите флажком топик и нажмите  кнопку **"Delete selected topics"**
3. Подтвердите удаление

---

## 💬 Просмотр и отправка сообщений

### Просмотр сообщений в топике

1. Откройте топик (кликните на его имя)
2. Перейдите на вкладку **"Messages"**
3. Настройте параметры просмотра:
   - **Partition** — выберите партицию (или "All partitions")
   - **Seek Type** — способ поиска:
     - `Earliest` — с самого начала
     - `Latest` — последние сообщения
     - `Offset` — с конкретного offset
     - `Timestamp` — с определённого времени
   - **Max Results** — максимальное количество сообщений (по умолчанию 100)
4. Нажмите **"Load messages"**

### Формат отображения сообщений

Каждое сообщение отображается с информацией:

- **Offset** — позиция сообщения в партиции
- **Timestamp** — время отправки
- **Key** — ключ сообщения (если есть)
- **Value** — содержимое сообщения
- **Headers** — заголовки сообщения (если есть)
- **Partition** — номер партиции

### Отправка сообщений (Producer)

1. На странице топика перейдите на вкладку **"Messages"**
2. Найдите раздел **"Produce Message"** или кнопку **"Produce"**
3. Заполните форму:
   - **Key** (опционально) — ключ сообщения
   - **Value** — содержимое сообщения
   - **Headers** (опционально) — заголовки в формате `key:value`
   - **Partition** — выберите партицию (или оставьте "Auto")
4. Нажмите **"Produce Message"**

### Примеры сообщений

#### Простое текстовое сообщение:

```
Key: (пусто)
Value: Hello, Kafka!
```

#### JSON сообщение:

```
Key: user-123
Value: {"name": "Иван", "age": 25, "city": "Москва"}
```

#### Сообщение с заголовками:

```
Key: order-456
Value: {"orderId": 456, "amount": 1500}
Headers:
 {
	"source": "web",
	"priority": "high"
}
```

---

## 👥 Работа с Consumer Groups

### Просмотр Consumer Groups

1. В меню слева выберите **"Consumers"** или **"Consumer Groups"**
2. Отобразится список всех consumer groups
3. Для каждой группы показывается:
   - Имя группы
   - Количество активных consumers
   - Количество топиков
   - Общий lag (отставание)

### Детальная информация о Consumer Group

1. Кликните на имя consumer group
2. Откроется страница с информацией:
   - **Overview** — общая информация
   - **Members** — активные consumers
   - **Topics** — топики, на которые подписана группа
   - **Offsets** — текущие offsets для каждой партиции
   - **Lag** — отставание по каждой партиции

### Понятие Lag

**Lag** — это разница между последним сообщением в топике и последним прочитанным сообщением consumer group.

- **Lag = 0** — consumer успевает обрабатывать все сообщения
- **Lag > 0** — consumer отстаёт от producer
- **Растущий Lag** — может указывать на проблемы с производительностью

### Сброс Offset (Reset Offset)

> ⚠️ **Внимание:** Сброс offset изменит позицию чтения для consumer group.

1. Откройте consumer group
2. Перейдите на вкладку **"Offsets"**
3. Выберите топик и партицию
4. Нажмите **"Reset Offset"**
5. Выберите стратегию:
   - **Earliest** — с начала топика
   - **Latest** — с последнего сообщения
   - **Custom** — конкретный offset
6. Подтвердите сброс

---

## 📊 Мониторинг и метрики

### Dashboard (Главная панель)

На главной странице отображается:

- **Количество топиков** — общее число топиков
- **Количество consumer groups** — активные группы
- **Количество брокеров** — узлы кластера
- **Общий размер данных** — объём хранимых данных
- **Throughput** — пропускная способность (сообщений/сек)

### Метрики топика

На странице топика доступны метрики:

- **Messages per second** — скорость поступления сообщений
- **Bytes per second** — скорость передачи данных
- **Total messages** — общее количество сообщений
- **Total size** — общий размер данных
- **Partition distribution** — распределение по партициям

### Метрики Consumer Group

- **Total lag** — общее отставание
- **Messages per second** — скорость обработки
- **Active consumers** — количество активных consumers

---

## ⚙️ Работа с брокерами (Brokers)

### Просмотр информации о брокерах

1. В меню выберите **"Brokers"**
2. Отобразится список брокеров в кластере
3. Для каждого брокера показывается:
   - **ID** — идентификатор брокера
   - **Host** — адрес хоста
   - **Port** — порт
   - **Rack** — стойка (если настроена)
   - **Status** — статус (Online/Offline)

### Конфигурация брокера

1. Кликните на брокер
2. Просмотрите конфигурацию:
   - **Basic settings** — основные настройки
   - **Advanced settings** — расширенные параметры
   - **Log directories** — директории для логов

---

## 🔍 Поиск и фильтрация

### Поиск топиков

1. На странице **Topics** используйте поле поиска
2. Введите имя топика или часть имени
3. Список автоматически отфильтруется

### Фильтрация сообщений

При просмотре сообщений можно использовать:

- **Фильтр по ключу** — поиск сообщений с определённым ключом
- **Фильтр по значению** — поиск по содержимому
- **Фильтр по заголовкам** — поиск по headers
- **Фильтр по времени** — сообщения за определённый период

---

## 🎓 Практические задания

### Задание 1: Создание топика и отправка сообщений

1. Создайте топик `student-tasks` с 1 партицией
2. Отправьте 5 сообщений через Kafka UI:
   - Сообщение 1: `"Задача 1: Изучить Kafka"`
   - Сообщение 2: `"Задача 2: Написать Producer"`
   - Сообщение 3: `"Задача 3: Написать Consumer"`
   - Сообщение 4: `"Задача 4: Использовать Kafka UI"`
   - Сообщение 5: `"Задача 5: Завершить лабораторную"`
3. Просмотрите все отправленные сообщения

### Задание 2: Работа с JSON сообщениями

1. Создайте топик `user-events`
2. Отправьте JSON сообщения:

```json
{"userId": 1, "event": "login", "timestamp": "2026-01-15T10:00:00Z"}
{"userId": 2, "event": "purchase", "amount": 1500, "timestamp": "2026-01-15T10:05:00Z"}
{"userId": 1, "event": "logout", "timestamp": "2026-01-15T10:10:00Z"}
```

3. Просмотрите сообщения и убедитесь, что JSON отображается корректно

### Задание 3: Мониторинг Consumer Group

1. Запустите consumer из предыдущей лабораторной работы:

```bash
python consumer.py
```

2. В Kafka UI:
   - Найдите consumer group `hello-group`
   - Просмотрите информацию о lag
   - Отправьте несколько сообщений через producer
   - Наблюдайте, как изменяется lag

### Задание 4: Анализ партиций

1. Создайте топик `multi-partition-topic` с 3 партициями
2. Отправьте 10 сообщений с ключами:
   - Ключи: `key-1`, `key-2`, `key-3` (чередуйте)
3. Просмотрите распределение сообщений по партициям
4. Объясните, почему сообщения распределились именно так

---

## 🐛 Решение проблем

### Проблема: Kafka UI не открывается

**Решение:**

1. Проверьте, запущен ли контейнер: `docker ps`
2. Проверьте логи: `docker logs kafka-ui`
3. Убедитесь, что порт 8080 не занят другим приложением

### Проблема: Не видно топиков

**Решение:**

1. Убедитесь, что Kafka запущен: `docker logs kafka`
2. Проверьте подключение к кластеру в Kafka UI
3. Попробуйте обновить страницу (F5)

### Проблема: Не удаётся отправить сообщение

**Решение:**

1. Проверьте, что топик существует
2. Убедитесь, что Kafka брокер доступен
3. Проверьте формат сообщения (особенно для JSON)

### Проблема: Consumer Group не отображается

**Решение:**

1. Убедитесь, что consumer запущен и подключён к Kafka
2. Consumer group появляется только после первого чтения сообщения
3. Проверьте, что используется правильный `group_id`

### Проблема: Ошибка 500 Internal Server Error при создании топика

**Симптомы:**

- При создании топика появляется ошибка: `500 Internal Server Error`
- Сообщение: `Error while creating AdminClient for Cluster local`
- Топик не появляется в списке после создания

**Причина:**
Kafka UI не может подключиться к Kafka брокеру, потому что `KAFKA_ADVERTISED_LISTENERS` настроен на `localhost:9092`, но внутри Docker сети нужно использовать имя сервиса `kafka:9092`.

**Решение:**

1. Проверьте конфигурацию в `docker-compose.yml`:

   ```yaml
   KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
   ```

   (должно быть `kafka:9092`, а не `localhost:9092`)

2. Перезапустите контейнеры:

   ```bash
   docker-compose restart
   ```

3. Подождите 10-15 секунд для полного запуска

4. Проверьте логи Kafka UI:

   ```bash
   docker logs kafka-ui --tail 20
   ```

   Не должно быть ошибок подключения к `localhost:9092`

5. Обновите страницу Kafka UI (F5)

**Примечание:** Если вы подключаетесь к Kafka с хоста (например, Python producer/consumer), используйте `localhost:9092`. Проброшенный порт работает независимо от `advertised.listeners` для первоначального подключения.

---

## 📌 Полезные советы

1. **Используйте фильтры** — при большом количестве сообщений используйте фильтрацию для поиска нужных данных

2. **Мониторьте Lag** — регулярно проверяйте lag consumer groups, чтобы вовремя обнаружить проблемы

3. **Изучайте метрики** — метрики помогают понять производительность системы

4. **Используйте JSON формат** — для структурированных данных используйте JSON, Kafka UI красиво его форматирует

5. **Сохраняйте важные сообщения** — перед удалением топика убедитесь, что важные данные сохранены

6. **Изучайте партиции** — понимание распределения по партициям важно для оптимизации производительности

---

## 📝 Контрольные вопросы

1. Какие основные разделы доступны в Kafka UI?
2. Как создать новый топик через веб-интерфейс?
3. Что такое **Lag** в контексте consumer groups?
4. Как отправить сообщение в топик через Kafka UI?
5. В чём разница между **Earliest** и **Latest** при просмотре сообщений?
6. Как можно отфильтровать сообщения в топике?
7. Что показывает метрика **Messages per second**?
8. Зачем нужен сброс offset в consumer group?

---

## 🎓 Вывод

В ходе изучения Kafka UI вы освоили:

- ✅ Навигацию по веб-интерфейсу
- ✅ Создание и управление топиками
- ✅ Просмотр и отправку сообщений
- ✅ Работу с consumer groups
- ✅ Мониторинг метрик и производительности
- ✅ Решение типичных проблем

Kafka UI значительно упрощает работу с Apache Kafka, предоставляя удобный графический интерфейс для всех основных операций.

---

## 📚 Дополнительные ресурсы

- **Официальная документация Kafka UI**: https://github.com/provectus/kafka-ui
- **Документация Apache Kafka**: https://kafka.apache.org/documentation/
- **Примеры использования**: https://github.com/provectus/kafka-ui/tree/master/documentation

---

**Удачной работы с Kafka UI! 🚀**
