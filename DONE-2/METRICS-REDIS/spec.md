1. нужно в докере поднять

postgres 17
mongodb
redis


2. Создать сервер фастапи в одной файле


с 3 мя эндпонинтами, в которых мы с помощью сваггер передаем 

общее количество запросов
количество одновременных (конкурентных) соединений (потоков)

мне нужен эндпоинт
в которых мы с помощью сваггер передаем 

общее количество запросов
количество одновременных (конкурентных) соединений (потоков)

и утилитой ab -n 1000 -c 50
тестируем производительность бд

return возвращает сколько данных было потеряно (не записано) 

посоветуй как решить эту исследовательскую задачу


тестировать будем таблицы  Counter для всех трех бд, референс для постгрес
class Counter(Base):
    __tablename__ = "counter"
    id = Column(Integer, primary_key=True, default=1)
    value = Column(Integer, default=0) 



